{{ form_start(form) }}
<fieldset class="form">
    <legend>Sukurti albumą</legend>
    {{ form_row(form.short_comment) }}
    {{ form_row(form.long_comment) }}
    {{ form_row(form.Ikelti) }}
</fieldset>
{{ form_end(form) }}
